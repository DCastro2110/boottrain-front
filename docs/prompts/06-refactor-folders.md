## Refactor Folders

## Logic Change

1. Move shared componentes from (home)/components to src/components/shared ou if layout components to src/components/layout
2. create a folder src/components/ai and move all ai related components to there
3. MOve error-page to src/components/layout and rename to error-container

## Requirements

- Rename using "git mv" to preserve git history
- Update all imports to reflect the new paths
- Each changed file should be committed separately with a clear commit message indicating the refactor performed. Use the commiter sugagented commit messages as a guide for writing your own commit messages.
