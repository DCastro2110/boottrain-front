# Task 3: Hero Section and Start Session Action

## Description

Implement the top hero section of the page, displaying the workout day's info and the action to start the session.

## Acceptance Criteria

- [ ] Implement the hero component based on design node `q3wGFH` (displaying title, duration, image, etc.).
- [ ] Implement the "Iniciar Treino" button within this section based on design node `J5Ubc`.
- [ ] When the button is clicked, execute a mutation to `postWorkoutPlanWorkoutPlanIdWorkoutDaysWorkoutDayIdSessions`.
- [ ] Handle loading states during the mutation and handle errors properly.
- [ ] Ensure the UI updates to reflect that the session is active (e.g., hiding the start button if appropriate, though standard behavior implies starting a timer or just enabling the finish button).
