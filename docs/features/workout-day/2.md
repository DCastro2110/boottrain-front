# Task 2: Data Fetching and Main Page Skeleton

## Description

Fetch the necessary data for the workout day on the server and set up the main responsive layout for the page.

## Acceptance Criteria

- [ ] Create a data-fetch function inside `src/data-fetch/get-workout-day.ts` following the existing pattern (e.g., `getHomeInfoData`).
- [ ] Use the Orval generated function `getWorkoutPlanWorkoutPlanIdWorkoutDaysWorkoutDayId` inside this new data-fetch function.
- [ ] The data MUST be fetched on the server (in a Server Component).
- [ ] Ensure the path parameters `id` (workoutPlanId) and `workoutDayId` are correctly passed to the API.
- [ ] Setup the main responsive container matching the design layout for node `l3fqk`.
- [ ] Handle error states gracefully, returning `null` or showing an error boundary if the fetch fails.
